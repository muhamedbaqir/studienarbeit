@startmindmap
+ Linux Kernel Resilience Patterns
++ Fault isolation / Bulkhead
+++ C "Modules" (Request <-> Header file <-> Function)
+++ Memory Zones
+++ Device Drivers do not access user space
+++ Different hardware privilege levels for userspace and kernel space
++ Queue based load-levelling
+++ Task Scheduling (Tasklets/work queues)
+++ IO Scheduling (e.g. bulk request queue)
++ Caching
+++ Virtual Memory (dirty pages)
++ Retry
+++ Bulk driver requests can be configured to (not) be retried
++ circuit break
+++ possible through dynamic driver (un-)registration
++ Graceful Fail
+++ Panic after N oopses
++ Fault Detection
+++ Oopses (Faulty Memory Accesses)
+++ Runtime Sanitizers (e.g. KASAN Address checking)
++ Health Checks
+++ IOCTL system call for drivers
++ Load Shedding
+++ SWAP memory
++ MISC
+++ Security
++++ Linux Security Modules (Kernel enforced security)
++++ Struct Layout Randomization
+++ Chaos Engineering (Fuzzing)
+++ Real-Time
++++ atomic types
++++ interrupt priorities
@endmindmap
